CHIP Nx {
    IN a, b;    
    OUT out;

    PARTS:
    Not(in=a, out=nota);
    Not(in=b, out=notb);
    And(a=a, b=notb, out=out1);
    And(a=b, b=nota, out=out2);
    Or(a=out1, b=out2, out=out);
}
